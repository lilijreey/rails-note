= form_for([@article, @article.comments.new]) do |f|
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :email
    = f.text_field :email
  .field
    = f.label :body
    = f.text_area :body
  .actions
    = f.submit 'Add'
