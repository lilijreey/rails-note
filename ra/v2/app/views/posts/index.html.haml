.list-group
  - @posts.each do |post| 
    %li.list-group-item
      .post_wrapper
        %h2.title
          = link_to post.title, post
        %p.date 
          = post.created_at.strftime("%B %d, %Y")

= link_to 'New Post', new_post_path
